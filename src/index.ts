
let Projects = [
{name : "padrão"},

]
function Task(title : string, description : string, dueDate : object, priority : number, done : boolean){

    let newNotes = [];
    let newChecklist = [];



    return {title, description, dueDate, priority, done, newNotes, newChecklist}
}

function weekRecurrentTask(frequency: number[]){

}

function newProject(name : string, description : string, images : string[]){

}

function gratefulDiary(){
    
    return [] 
}

function targetDayTasks(day : object){

}